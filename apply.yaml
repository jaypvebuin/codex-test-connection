version: 0.2

phases:
  install: 
    on-failure: ABORT
    commands: 
      - echo ".......Installing Terraform......."
      - sudo apt-get update && sudo apt-get install -y gnupg software-properties-common
      -  echo "Install the HashiCorp GPG key"
      - wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg
      - echo "Verify the GPG key's fingerprint"
      - gpg --no-default-keyring --keyring /usr/share/keyrings/hashicorp-archive-keyring.gpg --fingerprint
      - echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
      - sudo apt update
      - echo "sudo apt-get install terraform -y"
      - sudo apt-get install terraform -y 
      - echo ".....Terraform version......."
      - terraform --version
  pre_build: 
    on-failure: ABORT
    commands: 
      - echo ".......Terraform Init........."
      - cd live/
      - terraform init -backend-config=$env/backend.tfvars --upgrade
      # set env as codebuild variable 
      
  build:
    on-failure: ABORT
    commands: 
      - terraform apply tfplan.binary
      # - terraform destroy -var-file=$env/terraform.tfvars --auto-approve